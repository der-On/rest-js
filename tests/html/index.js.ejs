$(document).ready(function() {
  TestRunner.init();
  var testRunner = new TestRunner();

  <% files.forEach(function(file) { %>
    file = require('<%- file %>');

    for(var test in file) {
      testRunner.addTest(test, file[test]);
    }
  <% }) %>

  testRunner.runTests();
});